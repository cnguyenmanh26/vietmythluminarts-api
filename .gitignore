# 1. Thư viện (NẶNG - Không bao giờ push lên)
node_modules/

# 2. File bảo mật (QUAN TRỌNG - Chứa mật khẩu)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# 3. File log lỗi của npm/yarn
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# 4. Thư mục Build (Code sinh ra tự động)
dist/
build/
coverage/

# 5. File hệ thống (Của Mac/Windows sinh ra)
.DS_Store
Thumbs.db

# 6. File cấu hình Editor (VS Code, Jetbrains...)
.idea/
.vscode/
*.swp
*.ntvs*
*.njsproj
*.sln
*.sw?

# 7. Docker (nếu có file override chạy local)
docker-compose.override.yml